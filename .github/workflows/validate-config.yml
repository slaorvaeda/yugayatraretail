name: Validate Configuration & Secrets

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/validate-config.yml'
  schedule:
    # Run daily at 2 AM UTC to catch any issues
    - cron: '0 2 * * *'

env:
  NODE_VERSION: "20"
  BACKEND_SERVICE: yugayatra-backend
  FRONTEND_SERVICE: yugayatra-frontend
  GCP_REGION: us-central1
  GCP_PROJECT_ID: yugayatraretail
  GCP_ARTIFACT_REGISTRY: yugayatraretail
  BACKEND_PORT: 5001
  FRONTEND_PORT: 3000

jobs:
  validate-secrets:
    name: Validate Secrets & Variables
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Validate Required Secrets
        id: required-secrets
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
        run: |
          python <<'PY'
import json
import os
import sys
from pathlib import Path
from textwrap import shorten

REPO_ROOT = Path(".")
FALLBACK_GCP_KEY_FILE = REPO_ROOT / "GCP_SA_KEY_TO_COPY.json"

errors = 0
warnings = 0

def get_secret(name, *, required=True, preview=False, min_length=None, fallback_file=None):
    global errors, warnings
    value = os.environ.get(name)

    if (value is None or not value.strip()) and fallback_file:
        fallback_path = REPO_ROOT / fallback_file
        if fallback_path.exists():
            value = fallback_path.read_text(encoding="utf-8").strip()
            print(f"ℹ️  Using fallback file for {name}: {fallback_file}")

    if value is None or not value.strip():
        if required:
            print(f"❌ {name}: NOT SET (REQUIRED)")
            errors += 1
        else:
            print(f"⚠️  {name}: Not set (optional)")
        return None

    value = value.strip()
    if preview:
        print(f"✅ {name}: SET")
        print(f"   📋 Value: {shorten(value, width=50, placeholder='...')}")
    else:
        length = len(value)
        print(f"✅ {name}: SET (length: {length} chars)")
        if min_length and length < min_length:
            print(f"   ⚠️  WARNING: {name} should be at least {min_length} characters")
            warnings += 1

    return value

print("==========================================")
print("🔍 Validating Required Secrets")
print("==========================================")

# GCP_SA_KEY with fallback to repo file
gcp_sa_key = get_secret(
    "GCP_SA_KEY",
    fallback_file="GCP_SA_KEY_TO_COPY.json",
)

if gcp_sa_key:
    try:
        parsed = json.loads(gcp_sa_key)
    except json.JSONDecodeError as exc:
        print("   ❌ Invalid JSON format")
        print(f"   🔍 JSON Error: {exc}")
        print("   💡 Tip: Copy the entire JSON from github-actions-key.json")
        errors += 1
    else:
        project_id = parsed.get("project_id")
        client_email = parsed.get("client_email")
        print(f"   📋 Project ID: {project_id or '<missing>'}")
        print(f"   📋 Client Email: {client_email or '<missing>'}")

# SUPABASE_URL (preview)
get_secret("SUPABASE_URL", preview=True)

# SUPABASE_SERVICE_ROLE_KEY
get_secret("SUPABASE_SERVICE_ROLE_KEY")

# JWT_SECRET (min 32 chars)
get_secret("JWT_SECRET", min_length=32)

# CLOUDINARY secrets
get_secret("CLOUDINARY_CLOUD_NAME")
get_secret("CLOUDINARY_API_KEY")
get_secret("CLOUDINARY_API_SECRET")

print("")
print("==========================================")
print("📊 Required Secrets Summary")
print("==========================================")
print(f"Errors: {errors}")
print(f"Warnings: {warnings}")
print("==========================================")

github_output = os.environ.get("GITHUB_OUTPUT")
if github_output:
    with open(github_output, "a", encoding="utf-8") as gh:
        gh.write(f"errors={errors}\n")
        gh.write(f"warnings={warnings}\n")

if errors > 0:
    print("❌ Validation failed: required secret(s) missing or invalid")
    sys.exit(1)

print("✅ All required secrets are valid!")
PY

      - name: Validate Optional Secrets
        id: optional-secrets
        run: |
          echo "=========================================="
          echo "🔍 Validating Optional Secrets"
          echo "=========================================="
          
          # Check GCP_REGION
          GCP_REGION="${{ secrets.GCP_REGION }}"
          if [ -z "$GCP_REGION" ] || [ "$GCP_REGION" = "" ]; then
            echo "⚠️  GCP_REGION: Not set (will use default: ${{ env.GCP_REGION }})"
          else
            echo "✅ GCP_REGION: $GCP_REGION"
          fi
          
          # Check GCP_PROJECT_ID
          GCP_PROJECT_ID="${{ secrets.GCP_PROJECT_ID }}"
          if [ -z "$GCP_PROJECT_ID" ] || [ "$GCP_PROJECT_ID" = "" ]; then
            echo "⚠️  GCP_PROJECT_ID: Not set (will use default: ${{ env.GCP_PROJECT_ID }})"
          else
            echo "✅ GCP_PROJECT_ID: $GCP_PROJECT_ID"
          fi
          
          # Check GCP_ARTIFACT_REGISTRY
          GCP_ARTIFACT_REGISTRY="${{ secrets.GCP_ARTIFACT_REGISTRY }}"
          if [ -z "$GCP_ARTIFACT_REGISTRY" ] || [ "$GCP_ARTIFACT_REGISTRY" = "" ]; then
            echo "⚠️  GCP_ARTIFACT_REGISTRY: Not set (will use default: ${{ env.GCP_ARTIFACT_REGISTRY }})"
          else
            echo "✅ GCP_ARTIFACT_REGISTRY: $GCP_ARTIFACT_REGISTRY"
          fi
          
          # Check CLIENT_URL
          CLIENT_URL=$(printf '%s' "${{ secrets.CLIENT_URL }}")
          if [ -z "${CLIENT_URL}" ]; then
            echo "⚠️  CLIENT_URL: Not set (will be set automatically after frontend deployment)"
          else
            echo "✅ CLIENT_URL: ${CLIENT_URL}"
          fi
          
          # Check NEXT_PUBLIC_API_BASE_URL
          NEXT_PUBLIC_API_BASE_URL=$(printf '%s' "${{ secrets.NEXT_PUBLIC_API_BASE_URL }}")
          if [ -z "${NEXT_PUBLIC_API_BASE_URL}" ]; then
            echo "⚠️  NEXT_PUBLIC_API_BASE_URL: Not set (will use backend URL after deployment)"
          else
            echo "✅ NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL}"
          fi
          
          echo "=========================================="

      - name: Validate Environment Variables
        run: |
          echo "=========================================="
          echo "🔍 Validating Environment Variables"
          echo "=========================================="
          
          echo "✅ NODE_VERSION: ${{ env.NODE_VERSION }}"
          echo "✅ BACKEND_SERVICE: ${{ env.BACKEND_SERVICE }}"
          echo "✅ FRONTEND_SERVICE: ${{ env.FRONTEND_SERVICE }}"
          echo "✅ GCP_REGION: ${{ env.GCP_REGION }}"
          echo "✅ GCP_PROJECT_ID: ${{ env.GCP_PROJECT_ID }}"
          echo "✅ GCP_ARTIFACT_REGISTRY: ${{ env.GCP_ARTIFACT_REGISTRY }}"
          echo "✅ BACKEND_PORT: ${{ env.BACKEND_PORT }}"
          echo "✅ FRONTEND_PORT: ${{ env.FRONTEND_PORT }}"
          
          echo "=========================================="

      - name: Validate Image Tag Format
        run: |
          echo "=========================================="
          echo "🔍 Validating Image Tag Format"
          echo "=========================================="
          
          # Get values with fallbacks
          REGION="${{ secrets.GCP_REGION }}"
          PROJECT="${{ secrets.GCP_PROJECT_ID }}"
          REPO="${{ secrets.GCP_ARTIFACT_REGISTRY }}"
          
          REGION=$(echo "$REGION" | xargs)
          PROJECT=$(echo "$PROJECT" | xargs)
          REPO=$(echo "$REPO" | xargs)
          
          if [ -z "$REGION" ] || [ "$REGION" = "null" ] || [ "$REGION" = "" ]; then
            REGION="${{ env.GCP_REGION }}"
          fi
          
          if [ -z "$PROJECT" ] || [ "$PROJECT" = "null" ] || [ "$PROJECT" = "" ]; then
            PROJECT="${{ env.GCP_PROJECT_ID }}"
          fi
          
          if [ -z "$REPO" ] || [ "$REPO" = "null" ] || [ "$REPO" = "" ]; then
            REPO="${{ env.GCP_ARTIFACT_REGISTRY }}"
          fi
          
          # Build sample tags
          BACKEND_TAG="${REGION}-docker.pkg.dev/${PROJECT}/${REPO}/backend:test"
          FRONTEND_TAG="${REGION}-docker.pkg.dev/${PROJECT}/${REPO}/frontend:test"
          
          echo "Backend tag format: $BACKEND_TAG"
          echo "Frontend tag format: $FRONTEND_TAG"
          
          # Validate tags don't start with dash
          if [[ "$BACKEND_TAG" == -* ]] || [[ "$FRONTEND_TAG" == -* ]]; then
            echo "❌ ERROR: Image tag starts with dash! REGION is missing."
            exit 1
          fi
          
          # Validate format
          if [[ ! "$BACKEND_TAG" =~ ^[a-z0-9-]+-docker\.pkg\.dev/[a-z0-9-]+/[a-z0-9-]+/backend:.+$ ]]; then
            echo "❌ ERROR: Invalid backend tag format"
            exit 1
          fi
          
          if [[ ! "$FRONTEND_TAG" =~ ^[a-z0-9-]+-docker\.pkg\.dev/[a-z0-9-]+/[a-z0-9-]+/frontend:.+$ ]]; then
            echo "❌ ERROR: Invalid frontend tag format"
            exit 1
          fi
          
          echo "✅ Image tag format is valid"
          echo "=========================================="

  validate-gcp-connection:
    name: Validate GCP Connection
    runs-on: ubuntu-latest
    needs: validate-secrets
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}

      - name: Validate GCP Authentication
        run: |
          echo "=========================================="
          echo "🔍 Validating GCP Authentication"
          echo "=========================================="
          
          # Get current account
          ACCOUNT=$(gcloud auth list --filter=status:ACTIVE --format="value(account)")
          if [ -z "$ACCOUNT" ]; then
            echo "❌ No active GCP account found"
            exit 1
          fi
          
          echo "✅ Authenticated as: $ACCOUNT"
          echo "=========================================="

      - name: Validate GCP Project Access
        run: |
          echo "=========================================="
          echo "🔍 Validating GCP Project Access"
          echo "=========================================="
          
          # Get values with fallbacks
          REGION="${{ secrets.GCP_REGION }}"
          PROJECT="${{ secrets.GCP_PROJECT_ID }}"
          
          REGION=$(echo "$REGION" | xargs)
          PROJECT=$(echo "$PROJECT" | xargs)
          
          if [ -z "$REGION" ] || [ "$REGION" = "null" ] || [ "$REGION" = "" ]; then
            REGION="${{ env.GCP_REGION }}"
          fi
          
          if [ -z "$PROJECT" ] || [ "$PROJECT" = "null" ] || [ "$PROJECT" = "" ]; then
            PROJECT="${{ env.GCP_PROJECT_ID }}"
          fi
          
          # Set project
          gcloud config set project $PROJECT
          
          # Verify project access
          PROJECT_INFO=$(gcloud projects describe $PROJECT --format="value(projectId,name)" 2>&1)
          if [ $? -ne 0 ]; then
            echo "❌ Cannot access project: $PROJECT"
            echo "$PROJECT_INFO"
            exit 1
          fi
          
          echo "✅ Project accessible: $PROJECT"
          echo "   Project info: $PROJECT_INFO"
          
          # Check if required APIs are enabled
          echo ""
          echo "Checking required APIs..."
          
          # Cloud Run API
          if gcloud services list --enabled --filter="name:run.googleapis.com" --format="value(name)" | grep -q "run.googleapis.com"; then
            echo "✅ Cloud Run API: Enabled"
          else
            echo "❌ Cloud Run API: Not enabled"
            exit 1
          fi
          
          # Artifact Registry API
          if gcloud services list --enabled --filter="name:artifactregistry.googleapis.com" --format="value(name)" | grep -q "artifactregistry.googleapis.com"; then
            echo "✅ Artifact Registry API: Enabled"
          else
            echo "❌ Artifact Registry API: Not enabled"
            exit 1
          fi
          
          echo "=========================================="

      - name: Validate Artifact Registry Access
        run: |
          echo "=========================================="
          echo "🔍 Validating Artifact Registry Access"
          echo "=========================================="
          
          # Get values with fallbacks
          REGION="${{ secrets.GCP_REGION }}"
          PROJECT="${{ secrets.GCP_PROJECT_ID }}"
          REPO="${{ secrets.GCP_ARTIFACT_REGISTRY }}"
          
          REGION=$(echo "$REGION" | xargs)
          PROJECT=$(echo "$PROJECT" | xargs)
          REPO=$(echo "$REPO" | xargs)
          
          if [ -z "$REGION" ] || [ "$REGION" = "null" ] || [ "$REGION" = "" ]; then
            REGION="${{ env.GCP_REGION }}"
          fi
          
          if [ -z "$PROJECT" ] || [ "$PROJECT" = "null" ] || [ "$PROJECT" = "" ]; then
            PROJECT="${{ env.GCP_PROJECT_ID }}"
          fi
          
          if [ -z "$REPO" ] || [ "$REPO" = "null" ] || [ "$REPO" = "" ]; then
            REPO="${{ env.GCP_ARTIFACT_REGISTRY }}"
          fi
          
          # Configure Docker
          gcloud auth configure-docker ${REGION}-docker.pkg.dev --quiet
          
          # Check if repository exists
          REPO_PATH="${REGION}-docker.pkg.dev/${PROJECT}/${REPO}"
          if gcloud artifacts repositories describe $REPO --location=$REGION --format="value(name)" 2>/dev/null | grep -q "$REPO"; then
            echo "✅ Artifact Registry repository exists: $REPO_PATH"
          else
            echo "⚠️  Artifact Registry repository not found: $REPO_PATH"
            echo "   Repository will be created on first push"
          fi
          
          echo "=========================================="

      - name: Validate Cloud Run Services
        run: |
          echo "=========================================="
          echo "🔍 Validating Cloud Run Services"
          echo "=========================================="
          
          # Get values with fallbacks
          REGION="${{ secrets.GCP_REGION }}"
          PROJECT="${{ secrets.GCP_PROJECT_ID }}"
          BACKEND_SERVICE="${{ env.BACKEND_SERVICE }}"
          FRONTEND_SERVICE="${{ env.FRONTEND_SERVICE }}"
          
          REGION=$(echo "$REGION" | xargs)
          PROJECT=$(echo "$PROJECT" | xargs)
          
          if [ -z "$REGION" ] || [ "$REGION" = "null" ] || [ "$REGION" = "" ]; then
            REGION="${{ env.GCP_REGION }}"
          fi
          
          if [ -z "$PROJECT" ] || [ "$PROJECT" = "null" ] || [ "$PROJECT" = "" ]; then
            PROJECT="${{ env.GCP_PROJECT_ID }}"
          fi
          
          # Check backend service
          if gcloud run services describe $BACKEND_SERVICE --region=$REGION --format="value(metadata.name)" 2>/dev/null | grep -q "$BACKEND_SERVICE"; then
            echo "✅ Backend service exists: $BACKEND_SERVICE"
            BACKEND_URL=$(gcloud run services describe $BACKEND_SERVICE --region=$REGION --format="value(status.url)" 2>/dev/null)
            if [ -n "$BACKEND_URL" ]; then
              echo "   URL: $BACKEND_URL"
            fi
          else
            echo "ℹ️  Backend service not found: $BACKEND_SERVICE (will be created on first deployment)"
          fi
          
          # Check frontend service
          if gcloud run services describe $FRONTEND_SERVICE --region=$REGION --format="value(metadata.name)" 2>/dev/null | grep -q "$FRONTEND_SERVICE"; then
            echo "✅ Frontend service exists: $FRONTEND_SERVICE"
            FRONTEND_URL=$(gcloud run services describe $FRONTEND_SERVICE --region=$REGION --format="value(status.url)" 2>/dev/null)
            if [ -n "$FRONTEND_URL" ]; then
              echo "   URL: $FRONTEND_URL"
            fi
          else
            echo "ℹ️  Frontend service not found: $FRONTEND_SERVICE (will be created on first deployment)"
          fi
          
          echo "=========================================="

  validate-docker-builds:
    name: Validate Docker Builds
    runs-on: ubuntu-latest
    needs: validate-secrets
    timeout-minutes: 20

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Validate Backend Dockerfile
        run: |
          echo "=========================================="
          echo "🔍 Validating Backend Dockerfile"
          echo "=========================================="
          
          if [ ! -f "./backend/Dockerfile" ]; then
            echo "❌ Backend Dockerfile not found"
            exit 1
          fi
          
          echo "✅ Backend Dockerfile exists"
          
          # Check if Dockerfile is valid (basic syntax check)
          if docker buildx build --dry-run -f ./backend/Dockerfile ./backend 2>&1 | grep -q "error"; then
            echo "❌ Backend Dockerfile has syntax errors"
            exit 1
          fi
          
          echo "✅ Backend Dockerfile syntax is valid"
          echo "=========================================="

      - name: Validate Frontend Dockerfile
        run: |
          echo "=========================================="
          echo "🔍 Validating Frontend Dockerfile"
          echo "=========================================="
          
          if [ ! -f "./frontend/Dockerfile" ]; then
            echo "❌ Frontend Dockerfile not found"
            exit 1
          fi
          
          echo "✅ Frontend Dockerfile exists"
          
          # Check if Dockerfile is valid (basic syntax check)
          if docker buildx build --dry-run -f ./frontend/Dockerfile ./frontend 2>&1 | grep -q "error"; then
            echo "❌ Frontend Dockerfile has syntax errors"
            exit 1
          fi
          
          echo "✅ Frontend Dockerfile syntax is valid"
          echo "=========================================="

      - name: Test Backend Docker Build (Dry Run)
        run: |
          echo "=========================================="
          echo "🔍 Testing Backend Docker Build"
          echo "=========================================="
          
          # Test build without pushing
          docker buildx build \
            --load \
            --tag test-backend:latest \
            -f ./backend/Dockerfile \
            ./backend 2>&1 | head -20
          
          if [ $? -eq 0 ]; then
            echo "✅ Backend Docker build test passed"
          else
            echo "❌ Backend Docker build test failed"
            exit 1
          fi
          
          echo "=========================================="

      - name: Test Frontend Docker Build (Dry Run)
        run: |
          echo "=========================================="
          echo "🔍 Testing Frontend Docker Build"
          echo "=========================================="
          
          # Test build without pushing
          docker buildx build \
            --load \
            --tag test-frontend:latest \
            -f ./frontend/Dockerfile \
            --build-arg NEXT_PUBLIC_API_BASE_URL=http://localhost:5001/api \
            ./frontend 2>&1 | head -20
          
          if [ $? -eq 0 ]; then
            echo "✅ Frontend Docker build test passed"
          else
            echo "❌ Frontend Docker build test failed"
            exit 1
          fi
          
          echo "=========================================="

  validation-summary:
    name: Validation Summary
    runs-on: ubuntu-latest
    needs: [validate-secrets, validate-gcp-connection, validate-docker-builds]
    if: always()

    steps:
      - name: Generate Summary
        run: |
          echo "=========================================="
          echo "📊 VALIDATION SUMMARY"
          echo "=========================================="
          echo ""
          echo "✅ Secrets Validation: ${{ needs.validate-secrets.result }}"
          echo "✅ GCP Connection: ${{ needs.validate-gcp-connection.result }}"
          echo "✅ Docker Builds: ${{ needs.validate-docker-builds.result }}"
          echo ""
          
          if [ "${{ needs.validate-secrets.result }}" = "success" ] && \
             [ "${{ needs.validate-gcp-connection.result }}" = "success" ] && \
             [ "${{ needs.validate-docker-builds.result }}" = "success" ]; then
            echo "🎉 ALL VALIDATIONS PASSED!"
            echo ""
            echo "Your configuration is ready for deployment."
            echo "You can now run the deploy-gcp.yml workflow."
          else
            echo "❌ SOME VALIDATIONS FAILED"
            echo ""
            echo "Please check the failed jobs above and fix the issues."
            exit 1
          fi
          
          echo "=========================================="

